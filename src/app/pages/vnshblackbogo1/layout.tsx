export default function VnshBlackBogo1Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="w-full bg-white px-4 md:px-8 lg:px-[70px] max-w-[1265px] mx-auto relative overflow-hidden">
      {children}
    </div>
  );
}
